{
  "name": "simple-deploy-test",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": "^8",
    "@sap/xssec": "^4",
    "express": "^4"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "auth": {
        "kind": "mocked"
      }
    }
  },
  "devDependencies": {
    "@sap/cds-dk": "^9.7.2"
  }
}
