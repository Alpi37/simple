_schema-version: "3.1"
ID: simple-deploy-test
version: 1.0.7

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
        - npx cds build --production

modules:
  - name: simple-deploy-test-srv
    type: nodejs
    path: gen/srv
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: custom
      commands: [] # Nichts mehr tun, nur einsammeln
      supported-platforms: [cf]