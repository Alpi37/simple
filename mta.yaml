_schema-version: "3.1"
ID: simple-deploy-test
version: 1.0.0
modules:
  - name: simple-deploy-test-srv
    type: nodejs
    path: gen/srv
    parameters:
      memory: 512M
      disk-quota: 512M
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}